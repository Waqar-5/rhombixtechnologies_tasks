# Not required for local dev (Vite proxy handles /api and /uploads -> http://localhost:5000)
# Add VITE_ prefixed vars here if you deploy client and server separately, e.g.:
# VITE_API_URL=https://api.yourapp.com
